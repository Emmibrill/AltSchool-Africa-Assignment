-- Delete queries for inventory management system

-- 1. Delete a user by user_id
DELETE FROM users
WHERE user_id = 4;  -- Delete user with user_id 4

-- 2. Delete a category by category_id
DELETE FROM categories
WHERE category_id = 5;  -- Delete category with category_id 5

